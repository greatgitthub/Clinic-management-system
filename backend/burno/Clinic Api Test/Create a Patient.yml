info:
  name: Create a Patient
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/patients
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​