info:
  name: Test AI
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5001/predict
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​